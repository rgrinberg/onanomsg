begin test "test_opts"
  files = [ "test_opts.ml" ]
  requires = [ "onanomsg" ]
end

begin test "test_pubsub"
  files = [ "test_pubsub.ml" ]
  requires = [ "onanomsg" ]
end

begin test "test_reqrep"
  files = [ "test_reqrep.ml" ]
  requires = [ "onanomsg" ]
end

begin test "test_endpoint"
  files = [ "test_endpoint.ml" ]
  requires = [ "onanomsg" ]
end
